# -*- shell-script -*-

export PS1="$ "
export HISTFILE=/tmp/history

terminal ()
{
    gnome-terminal \
        --window \
        --geometry=+100+100 \
        --hide-menubar \
        --title='Tech Talk PSE Simple example' \
        -e '/bin/bash --norc'
}
